# 1%CLUB 콘텐츠 제작 정책

## 이미지

- 한 브랜드의 8장 세트 안에서 같은 원본 사진은 한 번만 사용한다.
- 표지는 `cover` 전용 이미지 배정만 사용하며 `product`, `heroshot` 등 본문 카드 이미지를 대신 가져오지 않는다.
- 고유 후보가 소진되면 이미 사용한 사진을 반복하지 않고 해당 카드 전용 생성 배경을 사용한다.
- 사용자가 같은 이미지를 다른 카드에 다시 올리면 적용하지 않고 중복 안내를 표시한다.
- 표지는 한 장의 세로 이미지를 풀블리드로 사용하는 매거진 커버 구도를 기본으로 한다. 누끼 이미지는 같은 프레임 안에서 하나의 오브제로만 배치한다.

## 1%CLUB 로고

- 로고 자리에서 `1%CLUB`, `1% CLUB`, `1CLUB`을 텍스트로 조판하지 않는다.
- 반드시 공식 1%CLUB 로고 이미지의 흰색 또는 검은색 버전을 사용한다.
- 한 프레임에는 공식 로고를 한 번만, 작게 배치하고 충분한 안전 여백을 둔다.
- 계정 핸들(`@1club.kr`), 해시태그(`#1CLUB`), 설명 문장처럼 로고가 아닌 텍스트 정보는 예외다.

## 검수

- 브랜드 세트의 최종 렌더 소스가 모두 고유한지 확인한다.
- 표지 이미지와 표지 크레딧이 같은 자산에서 나오는지 확인한다.
- 모바일 미리보기와 2160×2700 PNG에서 마스트헤드, 피사체, 하단 타이틀이 겹치지 않아야 한다.
